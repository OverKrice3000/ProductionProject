## Notification entity

This entity handles the display of notifications, providing a list component to show alerts and messages to the user.

### Public api

#### Components

- `NotificationList` - A component displaying a notification list.

#### Types

- `AppNotification` - A type describing content of a notification
