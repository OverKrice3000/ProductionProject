import { counterReducer } from "./model/slice/counterSlice";
import { Counter } from "./ui/Counter";

import type { CounterRootSchema } from "./model/types/counterSchema";

export { counterReducer, Counter, CounterRootSchema };
