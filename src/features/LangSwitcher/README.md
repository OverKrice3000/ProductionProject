## LangSwitcher feature

This feature provides a component for switching the application's language, allowing users to toggle between different locales.

### Public api

#### Components

- `LangSwitcher` - A dropdown component used to change the current language of the interface.
