## ScrollToTopButton feature

This feature provides a button that allows users to quickly navigate back to the top of the page.

### Public api

#### Components

- `ScrollToTopButton` - A button component that appears when scrolling and returns the user to the page header when clicked.
